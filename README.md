# GitSecure
git crawler that gathers statistics on C vulnerabilities of public repos
run:
Create personal access token on GitHub https://github.com/settings/applications
In your shell,first run
export P_TOKEN=your_personal_token
run python work_flow.py
